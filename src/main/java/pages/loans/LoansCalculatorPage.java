package pages.loans;

import org.openqa.selenium.WebDriver;
import pages.base.BasePage;

public class LoansCalculatorPage extends BasePage {
    public LoansCalculatorPage(WebDriver driver) {
        super(driver);
    }
}
